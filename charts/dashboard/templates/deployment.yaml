apiVersion: apps/v1    
kind: Deployment    
metadata:    
  name: {{ .Chart.Name }}    
spec:    
  replicas: 1    
  selector:    
    matchLabels:    
      app: {{ .Chart.Name }}    
  template:    
    metadata:    
      labels:    
        app: {{ .Chart.Name }}    
    spec:
      priorityClassName: reserved
      securityContext:
        allowPrivilegeEscalation: false
        runAsNonRoot: true
        runAsUser: 101
        runAsGroup: 101
        seccompProfile:
          type: RuntimeDefault
      containers:    
        - name: {{ .Chart.Name }}    
          image: xarthisius/wt-dashboard@sha256:3a26d4c6caf445fd1e746ac5e33e619cb99a9d3ad296da034f1dffe7b8e1862f
          ports:    
            - containerPort: 8080
          resources:
            limits:
              cpu: 750m
              memory: 768Mi
            requests:
              cpu: 500m
              ephemeral-storage: 50Mi
              memory: 512Mi
          env:    
            - name: GIRDER_API_URL
              value: {{ .Values.girderApiUrl | quote }}    
            - name: AUTH_PROVIDER    
              value: {{ .Values.authProvider }}
            - name: DATAONE_URL
              value: "https://cn.dataone.org/cn/v2"
            - name: RTD_URL
              value: {{ .Values.docsUrl }}
            - name: DASHBOARD_DEV
              value: {{ .Values.dashboardDev | quote }}
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
