service:
  port: 80
  protocol: http

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: {{ .Values.ingressClass }}
  hosts:
    - host: docs.{{ .Values.domain }}
  tls: 
    - secretName: wt-dashboard-tls
      hosts:
        - docs.{{ .Values.domain }}
