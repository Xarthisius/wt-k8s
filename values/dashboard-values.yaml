service:
  port: 80
  protocol: http

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: traefik
  hosts:
    - host: dashboard.local.wholetale.org
  tls: 
    - secretName: wt-dashboard-tls
      hosts:
        - dashboard.local.wholetale.org
